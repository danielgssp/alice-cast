displayText = (text) => 
{
    //document.getElementById('main').innerHTML = text;
    //var obj = JSON.parse(text);
    //executeAction({actionId: obj.actionId, data: obj.data});

    window.castReceiverManager.setApplicationState(obj);
};